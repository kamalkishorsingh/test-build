
name: deploy-test1
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'test1/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd test1 && VERSION=$(jq -r '.version' package.json) && echo "The version is $VERSION"
      - run: oldimage=`cat values.yaml | grep image | awk '{print$2}'` && echo $oldimage
      - run: cd test1 && sed -i 's/${oldimage}/${newtag}/g' values.yaml
