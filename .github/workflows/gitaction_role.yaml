name: deploy-test1
on:
  workflow_dispatch:
  push:
    branches:
      - new-master
    paths:
      - 'test1/**'
env:
  APP_FOLDER: test1
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Check out code
      uses: actions/checkout@v3
      
    - name: step1 Build, tag, and push image for docker hub
      run: |
        pwd
        echo "folder"
        cd "$APP_FOLDER"
        pwd
        ls -al
        brancename=$(git branch)
        echo "$brancename"
        newtag=$(jq -r '.version' package.json)
        echo "$newtag"
        echo "build step"

    repoupdate:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
    - name: Check out code
      uses: actions/checkout@v3
      with:
        repository: kamalkishorsingh/node
        ref: kktest
        path: test1
        HELM_REPO_UPDATE: ${{ secrets.HELM_REPO_UPDATE }}
    - name: Build, tag, and push image for docker hub
      run: |
        cd 
        cd test1
        ls -al
        newtag=$(jq -r '.version' package.json)
        echo "$newtag"
        cd 
        cd node/test1
        pwd

        


